# ウマスカウト 週次更新手順（自動更新エージェント用）

毎週土曜・日曜の朝8時(JST)に実行し、当日の中央競馬全レースのAI予想を生成して公開する。

## 手順

1. 今日の日付を `YYYYMMDD` 形式で確認する（JST基準）。
2. まず前回分の結果を集計する（未集計の過去開催日があれば自動で処理される）:
   ```
   python3 results.py --auto
   ```
3. データ収集を実行する:
   ```
   python3 scrape.py YYYYMMDD
   ```
   - 「開催なし」と出た場合は中央競馬の開催がない日なので、結果集計分だけcommit/pushして終了してよい。
   - 成功すると `data/YYYYMMDD.json` ができる。
   - **当日分のみ**を予想・公開する（翌日分を先に作らないこと。日曜の予想は日曜朝に公開する運用）。
4. `data/YYYYMMDD.json` を読み、**全レースを自分で予想**して `predictions/tmp_<会場ローマ字>.json` を会場ごとに書く（会場が3つなら3ファイル）。フォーマットは下記「予想JSONフォーマット」を厳守。
   - 分析観点: 過去5走の着順・上がり・通過順・相手関係、距離/コース適性、馬場、脚質とペース想定、騎手、斤量、間隔、オッズ(市場評価)。
   - 新馬戦は血統(父)・厩舎・騎手・オッズから判断。
   - **買い目は書かない**。各馬の確率の見立てだけを出す。買い目は build.py が機械的に組む。
   - レース数が多いので、サブエージェントを会場ごとに並列で使ってよい。
5. マージしてサイト用JSONを生成する（WIN5の買い目もこのとき自動生成される。対象レースはnetkeibaのWIN5ページ、取れなければ「各場最終レースを除く発走時刻の遅い5レース」で自動判定）:
   ```
   python3 build.py YYYYMMDD
   ```
   - 「警告: 予想が見つからないレース」が出たら、そのレースの予想を追加してからやり直す。
   - build.py が式別ごとに「投票方法」(`formations`)も作る。BOX・軸1頭ながし・1着固定といった、
     即PATで一括入力できる形の文字列で、サイトでは買い目一覧の上に表示される。
   - 着順を問わない式別（馬連・ワイド・三連複）は、組の中も一覧の並びも**必ず馬番の小さい順**。
     投票画面と見比べやすくするため。三連単だけは着順を表すのでその並びを保つ。
   - **公開済みの予想の買い目だけを組み直したいとき**（表示形式を変えた場合など）は
     `python3 build.py YYYYMMDD --rebuild-bets`。印に残っている確率から組み直すので、
     予想内容そのものは変わらない。
6. `predictions/tmp_*.json` を削除し、コミットしてpushする:
   ```
   rm predictions/tmp_*.json
   git add -A
   git commit -m "predict: YYYYMMDD"
   git push
   ```
7. push後、https://rossyrich.github.io/uma-scout/ に反映される（GitHub Pagesのビルドに1〜2分かかる）。

## 予想JSONフォーマット（predictions/tmp_*.json）

各レースについて、上位と見た **5〜6頭** の確率を見立てて書く。

```json
{
 "venue": "函館",
 "races": [
  {
   "race_id": "202602011201",
   "no": 1,
   "horses": [
    {"num": 5, "name": "馬名", "p_win": 32, "p_top3": 68, "comment": "評価理由を30字程度"},
    {"num": 8, "name": "馬名", "p_win": 18, "p_top3": 55, "comment": "..."},
    {"num": 2, "name": "馬名", "p_win": 12, "p_top3": 44, "comment": "..."},
    {"num": 11, "name": "馬名", "p_win": 9,  "p_top3": 38, "comment": "..."},
    {"num": 3, "name": "馬名", "p_win": 6,  "p_top3": 30, "comment": "..."}
   ],
   "summary": "展開予想と狙いを2〜3文",
   "confidence": "A"
  }
 ]
}
```

- `p_win` = その馬が**1着になる確率(%)**、`p_top3` = **3着以内に入る確率(%)**
- `confidence`: S(鉄板) / A(有力) / B(混戦) / C(荒れ模様)
- 番号はすべて馬番

### 確率を見立てるときの原則（重要）

過去187レースの検証で、**人気を覆した本命ほど大きく外している**ことが分かっている
（◎に1番人気を選んだとき1着率36%に対し、4〜6番人気を選んだときは1着率4%）。
以下を必ず守ること。

1. **人気馬を避けようとしない。** 1番人気が最も強いと判断したなら、素直に最も高い確率を与える。
   「人気だから割り引く」「妙味のある中穴を探す」といった調整はしない。
2. **オッズは市場の集合知として尊重する。** 覆すこと自体を目的にしない。市場と違う評価を
   するのは、過去走に明確な根拠（不利・距離不適・展開負けなど）があるときだけにする。
3. **確率は正直に。** 自信がないレースは全体的に低く平らな数字にする（例: 22/18/15/13/11）。
   自信があるときだけ尖らせる（例: 45/20/12/8/6）。無理に差をつけない。
4. **合計を意識する。** 挙げた馬の `p_win` の合計が100を超えないこと。`p_top3` は
   `p_win` 以上で、挙げた馬の合計がおおむね150〜250に収まるのが自然（3着まで=300%が全体）。
5. `p_top3` の順位づけを特に丁寧に。**買い目の大半（馬連・ワイド・三連複）はこの順で組まれる。**
- 全レース分を必ず出力（12R開催なら12レース）

## 開催中の途中経過（自動）

レースが確定するたびに予想画面へ結果を出すため、`results/live.json` を開催中に更新している。

- 更新役: Mac の launchd ジョブ `com.rossyrich.umascout.live`（15分おき / 土日9〜18時台のみ動作）
  - 実体は `live_update.sh` → `python3 results.py --live` → live.json を commit & push
  - 設定ファイル: `~/Library/LaunchAgents/com.rossyrich.umascout.live.plist`
  - ログ: `/tmp/umascout-live.log`
  - Macが起動していない間は更新されない（その日の最終結果は夜のGitHub Actionsが必ず拾う）
- `results.py --live` は発走時刻を過ぎた未取得レースだけを問い合わせるので数秒で終わる
- 確定版 `results/YYYYMMDD.json` ができた日は live.json を見に行かない（画面は確定版を優先）

手動で今すぐ反映したいときは:
```
python3 results.py --live && git add results/live.json && git commit -m "live" && git push
```
